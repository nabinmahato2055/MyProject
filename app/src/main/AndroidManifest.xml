<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rabintodo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/todoimage"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.example.rabintodo.Activity.TodoAddEditActivity"
            android:parentActivityName="com.example.rabintodo.Activity.HomeActivity"
            android:launchMode="singleTop"></activity>
        <activity android:name="com.example.rabintodo.Activity.HomeActivity" />
        <activity android:name="com.example.rabintodo.Activity.SplashActivity"/>
        <activity android:name="com.example.rabintodo.LoginSignUp.RegisterActivity" />
        <activity android:name="com.example.rabintodo.LoginSignUp.LoginActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>